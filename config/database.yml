# PostgreSQL
#   gem install pg
#
#   Ensure the pg gem is defined in your Gemfile
#   gem 'pg'
development:
  adapter: postgresql
  database: development
  # pool: 5
  # timeout: 5000
  host: localhost

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  adapter: postgresql
  database: test
  # pool: 5
  # timeout: 5000
  host: localhost

production:
  adapter: postgresql
  database: production
  # pool: 5
  # timeout: 5000
  host: http://still-ocean-8561.herokuapp.com/
